{% for vlan in device.site.vlan_groups.all()|sort(attribute='name') %}
  VLAN Group: {{ vlan.name }}
  {% for vlan in vlan.vlans.all()|sort(attribute='vid') %}
    VLAN ID: {{ vlan.vid }}
    VLAN Name: {{ vlan.name }}
    
    # Add any other VLAN-specific configuration here
    
  {% endfor %}
{% endfor %}