{% for vlan_group in vlan_groups %}
  {% for device in vlan_group.devices.all() %}
    {% if device.site == site %}
      VLAN Group: {{ vlan_group.name }}
      {% for vlan in vlan_group.vlans.all() %}
        VLAN ID: {{ vlan.vid }}
        VLAN Name: {{ vlan.name }}
        
        # Add any other VLAN-specific configuration here
        
      {% endfor %}
    {% endif %}
  {% endfor %}
{% endfor %}